function sumMix(x){
  sum = 0;
 newCollection = x.map((value) => sum += parseInt(value));
   return sum;
 }


betterSolution 
function sumMix(x){
  sum = 0;
  
  x.forEach(element => sum += parseInt(element));

   return sum;
 }
